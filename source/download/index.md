---
title: 资源下载
description: 这里收集了我分享的各种干货资源，每个资源都提供多种下载方式，挑你方便的来。
layout: download
resources:
  - name: 360宽带测速单文件版
    size: 3.9MB
    links:
      - type: lanzou     # 蓝奏云
        name: 下载
        url: https://suoma.lanzout.com/ileKX29jidna

  - name: PC版字体压缩生成.woff2字体
    size: 12MB
    links:
      - type: lanzou     # 蓝奏云
        name: 下载
        url: https://suoma.lanzout.com/iKRYT3ibopda
---

<!--
  这是下载站独立页。资源全部写在上面的 front-matter 里，
  以后想加资源，照着格式在 resources: 下面加一段就行，
  不用碰任何代码。

  字段说明：
    name   资源名称（必填）
    desc   一句话描述（可选，显示在名称下方）
    icon   封面图地址（可选，不填就用 emoji）
    emoji  不填 icon 时显示的 emoji（可选，默认 📦）
    tags   标签数组，用于搜索（可选）
    date   更新日期（可选，仅作记录，不在表格展示）
    size   大小，填到「大小」列（可选，不填显示 —）
    note   备注，填到「备注」列（可选，不填则显示 tags）
    links  下载方式列表（必填）
      type  下载类型：local / baidu / aliyun / weiyun / lanzou /
            ctfile / github / onedrive / google / other
      name  按钮上显示的文字（可选，不填按类型给默认名）
      url   下载地址（必填）
      pwd   提取码（网盘可选，填了会显示为 🔑xxxx 徽标）

  表格四列：软件名称 | 大小 | 立即下载 | 备注
  想用独立数据文件管理资源？删掉上面的 resources 段，
  改用 source/_data/download.yml（模板已附带示例），更易维护。
-->
